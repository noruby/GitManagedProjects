scattering_rate <- seq(150,380, length=50) #cm^-1 (<380 cm^-1 for grain size of >56nm)
plasma_frequency <-67900 #cm^-1
dielectric_background <- 1
speed_of_light <- 299792458 #m/s
operating_frequency <- 943 #cm^-1
epsilon <- dielectric_background - plasma_frequency^2 / ( operating_frequency^2 + 1i*operating_frequency*scattering_rate )
wavenumber <- operating_frequency* sqrt(epsilon/(1+epsilon)) #1/cm
propagation_length <- 1/(2*Im( wavenumber ))
png("plot.png", height=1000, width=1441, res=216)
plot(propagation_length)
dev.off()
plot(scattering_rate,propagation_length)
par()
plot(scattering_rate,propagation_length,type="l",xlab="Propagation length (cm)", ylab="Scattering rate (1/cm)")
plot(scattering_rate,propagation_length,type="l", xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
par(mar = c(2, 2, 1, 1)) #  余白の広さを行数で指定．下，左，上，右の順．
par(mgp = c(2.2, 0.7, 0)) #  余白の使い方．説明，ラベル，軸の位置を行で指定．
plot(scattering_rate,propagation_length,type="l", xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
par(mar = c(4, 4, 1, 1))
par(mar = c(4, 4, 1, 1)) #  余白の広さを行数で指定．下，左，上，右の順．
par(mgp = c(2.2, 0.7, 0)) #  余白の使い方．説明，ラベル，軸の位置を行で指定．
plot(scattering_rate,propagation_length,type="l", xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
plot(scattering_rate,propagation_length,type="l",col="red",xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
sqrt(1+1i)
sqrt(1i)
plot(scattering_rate,propagation_length,type="l",lwd=2, tcl=0.5, col="red",xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
png("plot.png", height=1000, width=1441, res=216)
par(mar = c(3, 3, 1, 1)) #  余白の広さを行数で指定．下，左，上，右の順．
par(mgp = c(1.7, 0.4, 0)) #  余白の使い方．説明，ラベル，軸の位置を行で指定．
plot(scattering_rate,propagation_length,type="l",lwd=2, tcl=0.5, col="red",xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
dev.off()
par(mar = c(2, 2, 1, 1)) #  余白の広さを行数で指定．下，左，上，右の順．
par(mgp = c(1.7, 0.4, 0)) #  余白の使い方．説明，ラベル，軸の位置を行で指定．
plot(scattering_rate,propagation_length,type="l",lwd=2, tcl=0.5, col="red",xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
par(mar = c(3, 3, 1, 1)) #  余白の広さを行数で指定．下，左，上，右の順．
par(mar = c(3, 3, 1, 1)) #  余白の広さを行数で指定．下，左，上，右の順．
par(mgp = c(1.5, 0.3, 0)) #  余白の使い方．説明，ラベル，軸の位置を行で指定．
plot(scattering_rate,propagation_length,type="l",lwd=2, tcl=0.5, col="red",xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
plot(scattering_rate,propagation_length,type="l",lwd=2, tcl=0.5, col="red",xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)", cex=1.5)
plot(scattering_rate,propagation_length,type="l",lwd=2, tcl=0.5, col="red",xlab="Scattering rate (1/cm)", ylab="Propagation length (cm)")
scattering_rate <- seq(150,380, length=50) #cm^-1 (<380 cm^-1 for grain size of >56nm)
plasma_frequency <-67900 #cm^-1
dielectric_background <- 1
speed_of_light <- 299792458 #m/s
operating_frequency <- 943 #cm^-1
epsilon <- dielectric_background - plasma_frequency^2 / ( operating_frequency^2 + 1i*operating_frequency*scattering_rate )
wavenumber_x <- operating_frequency* sqrt(epsilon/(1+epsilon)) #1/cm
propagation_length <- 1/(2*Im( wavenumber_x ))
#conductivity <- 
wavenumber_z_air <- operating_frequency* sqrt(1/(1+epsilon)) #1/cm
wavenumber_z_air
wavenumber_z_air <- operating_frequency* sqrt(1/(1+epsilon)) #1/cm
penetration_length_air <- 1/(2*Im( wavenumber_z_air ))
wavenumber_z_metal <- operating_frequency* sqrt(epsilon^2/(1+epsilon)) #1/cm
penetration_length_metal <- 1/(2*Im( wavenumber_z_metal ))
scattering_rate <- seq(150,380, length=50) #cm^-1 (<380 cm^-1 for grain size of >56nm)
plasma_frequency <-67900 #cm^-1
dielectric_background <- 1
speed_of_light <- 299792458 #m/s
operating_frequency <- 943 #cm^-1
epsilon <- dielectric_background - plasma_frequency^2 / ( operating_frequency^2 + 1i*operating_frequency*scattering_rate )
wavenumber_x <- operating_frequency* sqrt(epsilon/(1+epsilon)) #1/cm
propagation_length <- 1/(2*Im( wavenumber_x ))
#conductivity <- 
wavenumber_z_air <- operating_frequency* sqrt(1/(1+epsilon)) #1/cm
penetration_length_air <- 1/(2*Im( wavenumber_z_air ))
wavenumber_z_metal <- operating_frequency* sqrt(epsilon^2/(1+epsilon)) #1/cm
penetration_length_metal <- 1/(2*Im( wavenumber_z_metal ))
penetration_length_air
quit()
